/**
 * @deprecated Dữ liệu mock — toàn bộ luồng địa chỉ user dùng API `/users/addresses` qua addressService.
 * File giữ lại chỉ để tham khảo shape field; không import vào UI.
 */
export const MOCK_ADDRESSES = [];
