/** Mock đơn hàng — mặc định rỗng để hiển thị empty state; thay bằng API thật sau */
export const MOCK_ORDERS = [];

/**
 * Cấu trúc OrderCard (tham khảo khi tích hợp API):
 * {
 *   id: 'ORD-1024',
 *   shopName: 'Shop Auction Mall',
 *   status: 'completed',
 *   total: 1250000,
 *   createdAt: '2026-06-09T09:15:00',
 *   products: [{ id, name, image, quantity, price }]
 * }
 */
