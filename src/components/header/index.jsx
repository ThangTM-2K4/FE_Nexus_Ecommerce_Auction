import React from 'react';
import './index.scss';

const Header = () => {
  return (
    <header className="header">
      {/* TODO: Add header content */}
    </header>
  );
};

export default Header;
