import React from 'react';
import './index.scss';

const Footer = () => {
  return (
    <footer className="footer">
      {/* TODO: Add footer content */}
    </footer>
  );
};

export default Footer;
