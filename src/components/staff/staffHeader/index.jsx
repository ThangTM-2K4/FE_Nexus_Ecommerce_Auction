import React from 'react';
import './index.scss';

const StaffHeader = () => {
  return (
    <header className="staff-header">
      {/* TODO: Add staff header content */}
    </header>
  );
};

export default StaffHeader;
